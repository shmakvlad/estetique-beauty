# Собранные страницы — переформатирование ломает вёрстку:
# Prettier рвёт строчные элементы по строкам и добавляет пробелы там,
# где они влияют на раскладку (кнопки, иконки, метки в одну линию).
dist/

# Исходники фотографий
originals/

# Стили написаны компактно, по несколько свойств в строке.
# Prettier разложит их по одному на строку и файл вырастет втрое.
src/assets/site.css

package-lock.json
